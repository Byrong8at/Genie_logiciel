<Application x:Class="EasySave.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:EasySave"
             xmlns:viewModel="clr-namespace:EasySave.MVVM.ViewModel" 
             xmlns:view="clr-namespace:EasySave.MVVM.View">
    <Application.Resources>
        <DataTemplate DataType="{x:Type TypeName=viewModel:HomeViewModel}">
            <view:HomeView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type TypeName=viewModel:LanguageViewModel}">
            <view:LanguageView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type TypeName=viewModel:CreateViewModel}">
            <view:CreateView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type TypeName=viewModel:ExecuteViewModel}">
            <view:ExecuteView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type TypeName=viewModel:OverviewViewModel}">
            <view:OverviewView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type TypeName=viewModel:DeleteViewModel}">
            <view:DeleteView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type TypeName=viewModel:CheckViewModel}">
            <view:CheckView/>
        </DataTemplate>
        
    </Application.Resources>
</Application>
